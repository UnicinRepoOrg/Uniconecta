name: Struct8 Pipeline

on:
  push:
    paths:
      - '**/manifest.json'

permissions:
  id-token: write  
  contents: write    

jobs:
  infrastructure:
    name: Call GitOps Engine
    uses: Struct8/cloudman-core/.github/workflows/engine.yml@main
    secrets: inherit
